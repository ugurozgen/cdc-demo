# MM2 lab — Phase 1: 2 Kafka cluster + MirrorMaker 2 (gebze -> ankara, active/passive)
#
# gebze = primary/active      host: localhost:9092
# ankara = DR/passive         host: localhost:9094
#
# Container ağı içinde brokerlar kafka-a:19092 / kafka-b:19092 ile konuşur.
# Host'tan CLI için localhost:9092 (gebze) ve localhost:9094 (ankara).

name: mm2-lab

x-kafka-common: &kafka-common
  image: apache/kafka:3.9.1
  # Lab makinesinde bellek baskısıyla (exit 137) kapanmasın diye RAM sınırlanır.
  mem_limit: 1g
  healthcheck:
    test: ["CMD-SHELL", "/opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:19092 --list >/dev/null 2>&1 || exit 1"]
    interval: 5s
    timeout: 10s
    retries: 20
    start_period: 15s

services:
  kafka-a:
    <<: *kafka-common
    container_name: mm2-kafka-a
    ports:
      - "9092:9092"
    environment:
      KAFKA_HEAP_OPTS: "-Xmx512m -Xms256m"
      CLUSTER_ID: gebze-cluster-aaaaaaaaaa     # her cluster farklı ID -> bağımsız iki cluster
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller   # KRaft: tek node hem broker hem controller (zookeeper yok)
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka-a:9093
      # ÜÇ listener: CONTROLLER (KRaft iç), INTERNAL (container ağı), HOST (laptop'tan CLI).
      KAFKA_LISTENERS: CONTROLLER://:9093,INTERNAL://:19092,HOST://:9092
      # advertised = client'a "bana şu adresten bağlan" der. INTERNAL container adıyla,
      # HOST localhost ile. ⚠️ Container-İÇİ araçlar INTERNAL'a (19092) bağlanmalı; HOST'un
      # advertised'i (localhost:9092) container içinde erişilemez (lab'da yaşanan tuzak).
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka-a:19092,HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,HOST:PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL   # broker'lar arası trafik INTERNAL'dan
      # Tek broker olduğu için tüm iç topic'ler RF=1 (çok-node'da artır).
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0    # lab: consumer rebalance'ı bekletme

  kafka-b:
    # kafka-a ile birebir aynı yapı (env açıklamaları için yukarıya bak); farklar:
    # CLUSTER_ID, quorum voter hostu (kafka-b) ve HOST advertised portu (9094).
    <<: *kafka-common
    container_name: mm2-kafka-b
    ports:
      - "9094:9092"
    environment:
      KAFKA_HEAP_OPTS: "-Xmx512m -Xms256m"
      CLUSTER_ID: ankara-cluster-bbbbbbbbbb
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka-b:9093
      KAFKA_LISTENERS: CONTROLLER://:9093,INTERNAL://:19092,HOST://:9092
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka-b:19092,HOST://localhost:9094
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,HOST:PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0

  mm2:
    image: apache/kafka:3.9.1
    container_name: mm2-driver
    mem_limit: 768m
    environment:
      KAFKA_HEAP_OPTS: "-Xmx384m -Xms128m"
    depends_on:
      kafka-a:
        condition: service_healthy
      kafka-b:
        condition: service_healthy
    volumes:
      - ./mm2.properties:/opt/kafka/config/mm2.properties:ro
    entrypoint: ["/opt/kafka/bin/connect-mirror-maker.sh"]
    command: ["/opt/kafka/config/mm2.properties"]

  # ---------- Phase 2: CDC pipeline (gebze tarafı) ----------
  # pg-source(tablo) -> Debezium -> kafka-a -> JDBC sink -> pg-target(tablo)

  pg-source:
    image: postgres:17
    container_name: mm2-pg-source
    mem_limit: 512m
    command:
      - "postgres"
      - "-c"
      - "wal_level=logical"
      - "-c"
      - "max_wal_senders=10"
      - "-c"
      - "max_replication_slots=10"
      - "-c"
      - "hot_standby=on"
      - "-c"
      - "synchronized_standby_slots=standby_phys_slot"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sourcedb
      POSTGRES_HOST_AUTH_METHOD: trust   # lab: replication auth'u basit tut
    ports:
      - "5432:5432"
    volumes:
      - ./pg-hba-init.sh:/docker-entrypoint-initdb.d/00-pg-hba.sh:ro
      - ./sql/primary-init.sql:/docker-entrypoint-initdb.d/10-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d sourcedb"]
      interval: 5s
      timeout: 5s
      retries: 20

  pg-target:
    image: postgres:16
    container_name: mm2-pg-target
    mem_limit: 512m
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: targetdb
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d targetdb"]
      interval: 5s
      timeout: 5s
      retries: 20

  connect:
    image: quay.io/debezium/connect:3.0.0.Final
    container_name: mm2-connect
    mem_limit: 1g
    depends_on:
      kafka-a:
        condition: service_healthy
      pg-source:
        condition: service_healthy
      pg-target:
        condition: service_healthy
    ports:
      - "8084:8083"   # host 8083 başka container'da; lab REST -> localhost:8084
    environment:
      KAFKA_HEAP_OPTS: "-Xmx512m -Xms256m"
      BOOTSTRAP_SERVERS: kafka-a:19092
      GROUP_ID: dbz-connect-gebze
      CONFIG_STORAGE_TOPIC: connect_configs
      OFFSET_STORAGE_TOPIC: connect_offsets
      STATUS_STORAGE_TOPIC: connect_status
      CONFIG_STORAGE_REPLICATION_FACTOR: 1
      OFFSET_STORAGE_REPLICATION_FACTOR: 1
      STATUS_STORAGE_REPLICATION_FACTOR: 1
      KEY_CONVERTER: org.apache.kafka.connect.json.JsonConverter
      VALUE_CONVERTER: org.apache.kafka.connect.json.JsonConverter
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8083/ >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  # ---------- Phase 3: ankara tarafı stack (failback için) ----------
  # gebze ile simetrik: pg-source-b -> connect-b(Debezium) -> kafka-b -> connect-b(sink) -> pg-target-b
  # Normalde connector'lar burada PAUSED; failover'da resume edilir.

  pg-source-b:
    image: postgres:17
    container_name: mm2-pg-source-b
    mem_limit: 512m
    depends_on:
      pg-source:
        condition: service_healthy
    environment:
      PGDATA: /var/lib/postgresql/data
      PRIMARY_HOST: pg-source
    ports:
      - "5434:5432"
    volumes:
      - ./standby-entrypoint.sh:/standby-entrypoint.sh:ro
    entrypoint: ["/bin/bash", "/standby-entrypoint.sh"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d sourcedb"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 30s

  pg-target-b:
    image: postgres:16
    container_name: mm2-pg-target-b
    mem_limit: 512m
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: targetdb
    ports:
      - "5435:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d targetdb"]
      interval: 5s
      timeout: 5s
      retries: 20

  connect-b:
    image: quay.io/debezium/connect:3.0.0.Final
    container_name: mm2-connect-b
    mem_limit: 1g
    depends_on:
      kafka-b:
        condition: service_healthy
      pg-source-b:
        condition: service_healthy
      pg-target-b:
        condition: service_healthy
    ports:
      - "8085:8083"   # ankara Connect REST -> localhost:8085
    environment:
      KAFKA_HEAP_OPTS: "-Xmx512m -Xms256m"
      BOOTSTRAP_SERVERS: kafka-b:19092
      GROUP_ID: dbz-connect-ankara
      CONFIG_STORAGE_TOPIC: connect_configs
      OFFSET_STORAGE_TOPIC: connect_offsets
      STATUS_STORAGE_TOPIC: connect_status
      CONFIG_STORAGE_REPLICATION_FACTOR: 1
      OFFSET_STORAGE_REPLICATION_FACTOR: 1
      STATUS_STORAGE_REPLICATION_FACTOR: 1
      KEY_CONVERTER: org.apache.kafka.connect.json.JsonConverter
      VALUE_CONVERTER: org.apache.kafka.connect.json.JsonConverter
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8083/ >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  # ---------- Sürekli yük üreteci ----------
  # Aktif tarafın pg-source'una saniyede bir INSERT -> Debezium sürekli üretir, sink tüketir.
  # loadgen-a varsayılan (gebze aktif). failover'da loadgen-b devreye girer (profile).

  loadgen-a:
    image: postgres:16
    container_name: mm2-loadgen-a
    mem_limit: 128m
    depends_on:
      pg-source:
        condition: service_healthy
    environment:
      TARGET_HOST: pg-source
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        echo "loadgen -> $$TARGET_HOST"
        while true; do
          PGPASSWORD=postgres psql -h "$$TARGET_HOST" -U postgres -d sourcedb \
            -c "INSERT INTO inventory.customers(name,email) VALUES ('load-'||floor(random()*1000000)::int, 'load@example.com');" >/dev/null 2>&1 \
            || echo "insert bekliyor ($$TARGET_HOST hazır değil?)"
          sleep 1
        done

  loadgen-b:
    image: postgres:16
    container_name: mm2-loadgen-b
    mem_limit: 128m
    profiles: ["failover"]
    depends_on:
      pg-source-b:
        condition: service_healthy
    environment:
      TARGET_HOST: pg-source-b
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        echo "loadgen -> $$TARGET_HOST"
        while true; do
          PGPASSWORD=postgres psql -h "$$TARGET_HOST" -U postgres -d sourcedb \
            -c "INSERT INTO inventory.customers(name,email) VALUES ('load-'||floor(random()*1000000)::int, 'load@example.com');" >/dev/null 2>&1 \
            || echo "insert bekliyor ($$TARGET_HOST hazır değil?)"
          sleep 1
        done
